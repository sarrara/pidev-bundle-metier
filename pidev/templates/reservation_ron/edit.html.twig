{% extends 'base.html.twig' %}

{% block title %}Edit  Participation {% endblock %}

{% block body %}
    <section class="contact">
        <div class="container">
            <div class="row">
                <div class="col-sm-12 section-t8">
                    <div class="row">
                        <div class="col-md-7">
                            {{ form_start(form) }}

                            <div class="row">
                                <div class="col-md-6 mb-3">
                                    <div class="form-group">
                                        Nouriture : {{ form_widget(form.nourriture) }}
                                        {{ form_errors(form.nourriture) }}
                                    </div>
                                </div>
                                <div class="col-md-6 mb-3">
                                    <div class="form-group">
                                        Transport: {{ form_widget(form.transport) }}
                                        {{ form_errors(form.transport) }}
                                    </div>
                                </div>

                                <div class="col-md-12">
                                    <div class="form-group">Nombre de Place :
                                        {{ form_widget(form.nbrPersonne, { 'attr': {'class': 'form-control'} }) }}<br>
                                        {{ form_errors(form.nbrPersonne) }}
                                    </div>
                                </div>

                                <div class="col-md-12 text-center">
                                    <input type="submit" value="Enregistrer" class="btn btn-success" formnovalidate />
                                </div>
                            </div>
                            {{ form_end(form) }}

                        </div>
                    </div>
                </div>
            </div>
        </div>
    </section>
{% endblock %}
