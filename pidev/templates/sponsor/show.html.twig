{% extends 'base.html.twig' %}

{% block title %}Sponsor{% endblock %}

{% block body %}
    <h1>Sponsor</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ sponsor.id }}</td>
            </tr>
            <tr>
                <th>Nom</th>
                <td>{{ sponsor.nom }}</td>
            </tr>
            <tr>
                <th>Contrat</th>
                <td>{{ sponsor.contrat }}</td>
            </tr>
            <tr>
                <th>Description</th>
                <td>{{ sponsor.description }}</td>
            </tr>
        </tbody>
    </table>

    <a href="{{ path('sponsor_index') }}">back to list</a>

    <a href="{{ path('sponsor_edit', {'id': sponsor.id}) }}">edit</a>

    {{ include('sponsor/_delete_form.html.twig') }}
{% endblock %}
