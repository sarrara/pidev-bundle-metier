<form method="post" action="{{ path('activitie_delete', {'id': activitie.id}) }}" onsubmit="return confirm('Are you sure you want to delete this item?');">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ activitie.id) }}">
    <button class="btn">Delete</button>
</form>
